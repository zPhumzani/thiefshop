- page_header 'Listing users'

%table
  %thead
    %tr
      %th Login
      %th Password
      %th Email

  %tbody
    - @users.each do |user|
      %tr
        %td= user
        %td= user.email

= will_paginate @users